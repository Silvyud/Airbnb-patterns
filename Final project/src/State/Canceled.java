package State;

public class Canceled extends ReservationState {
    // Hereda todos los métodos con el mensaje de error por defecto.
    // Sin embargo, no se permite ninguna acción sobre una reserva cancelada.
}